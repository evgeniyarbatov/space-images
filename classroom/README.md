# Classroom packs

Run `make classroom` after you have local images (`make inspire` is enough).

Each pack is a markdown file with **5 images** (paths + kid/adult captions) and **5 discussion questions**.

Packs are written to `classroom/pack-YYYYMMDD/` and are local-only (gitignored).
